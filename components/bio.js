import { Box } from '@chakra-ui/react';
import styled from '@emotion/styled';

export const BioSecton = styled(Box)`
padding-left: 3.4 em; 
text-indent: -1.5 em;
`

export const BioYear = styled.span`
font-weight: bold;
margin-right: 1em;
`